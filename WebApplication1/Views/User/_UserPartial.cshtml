@using Microsoft.AspNet.Identity

@if (BusinessLayer.UserBissnessLayer.GetPermission((User.Identity.GetUserId<String>())) >= 2)
{
    @Html.ActionLink("Dodaj projekt", "UserManagementAdd", new { Id = this.ViewData["id"], AdmId = User.Identity.GetUserId<String>() }, null) <b>|</b>
    @Html.ActionLink("Usuń użytkownika", "UserManagementDelete", new { id = this.ViewData["id"]})

}