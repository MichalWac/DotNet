@using Microsoft.AspNet.Identity

    @Html.ActionLink("Przydziel projekt", "UserManagementAdd", new { Id = this.ViewData["id"], AdmId = User.Identity.GetUserId<String>() }, null)


@if (BusinessLayer.UserBissnessLayer.GetPermission((User.Identity.GetUserId<String>()))) {
    
    if ((String )this.ViewData["id"] != User.Identity.GetUserId<String>())
    {<b>|</b>
        @Html.ActionLink("Usuń użytkownika", "UserManagementDelete", new { id = this.ViewData["id"] })
    }
    
}
